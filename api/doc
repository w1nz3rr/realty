localhost/api/

-/auth - аутентификация (no method) +
--/login - вход (post) +
--/registration - регистрация (post) +


-/company - компании (нотариус | юрист | риелтор) (no method) +
--/(lawyer|realtor|notary|) -  список компаний (нотариус | юрист | риелтор) (get, post) ++
---/<company_id> - конкретная компания (get, put, delete) +++
продумать взаимодействие этих модулей
-/specialist - специалисты (no method)
--/(lawyer|realtor|notary|) - специалисты этой области (get, post)
---/<specialist_id> конкретный специалист (get, put, delete)


-/users - пользователи - (no method) +
--/<user_id> - посмотреть конкретного пользователя (get) +
---/complaints - жалоба (get, post) ++
---/blacklist - чёрный список (post, delete) +++


--/me - посмотреть себя (get, put, delete)
---/requisites - реквизиты (get, post)
----/<requisite_id> - конкретный реквизит (get, put, delete)
---/favourites - избранное (get, post)
----/<favourites_id> - конкретное объявление из избранного (get, delete)


--/chats - чаты (get, post)
---/<chat_id> - конкретный чат (get, delete)
----/message - все сообщения этого чата (get, post)
-----/<message_id> - конкретное сообщение (get, put, delete)


-/advertisements - объявления (get, post)
--/<advertisement_id> - конкретное объявление (get, put, delete)
---/reviews - отзывы (get, post)
----/<reviews_id> - конкретный отзыв (get, put, delete)


-/objects - объекты - (get, post)
--/<object_id> - конкретный объект (get, put, delete)
---/address - адрес объекта (get, put)
---/passport - паспорт объекта (get, put)
----/contractor - информация о подрядчике (get, put)
----/builder - информация о застройщике (get, put)


-/deals - сделки (get, post)
--/<deal_id> - сделка (get, put, delete)